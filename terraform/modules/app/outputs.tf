# retail_app_release_status output removed along with the retired
# helm_release.retail_app resource — see main.tf for why (upstream chart
# repo 404s; app is now deployed via plain manifests instead).
